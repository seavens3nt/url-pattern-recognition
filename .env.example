# Optional root-level Flask configuration; default command works without this file.
FLASK_APP=backend.app:create_app
FLASK_RUN_HOST=127.0.0.1
FLASK_RUN_PORT=5000
# Never place secrets in frontend environment variables.
